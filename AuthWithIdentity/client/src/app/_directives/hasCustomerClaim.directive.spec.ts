/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { HasCustomerClaimDirective } from './hasCustomerClaim.directive';

describe('Directive: HasCustomerClaim', () => {
  it('should create an instance', () => {
    const directive = new HasCustomerClaimDirective();
    expect(directive).toBeTruthy();
  });
});
